# AXONENGINE v4.0 - Core Dependencies
# ============================================
# OPTION B: Python 3.14 (EXPERIMENTAL)
# ============================================
# Status: NOT YET VERIFIED - Use at own risk
# Python: 3.14.0+
# Compatibility: UNKNOWN - Pre-release Python version
# Installation time: ~5-10 minutes (may require source builds)
# 
# USE THIS IF:
# - You have Python 3.14+ installed
# - You're testing new Python features
# - You don't need production stability
#
# WARNINGS:
# - Some packages may not have 3.14 wheels yet
# - RDKit may require compilation from source
# - LangChain/LangGraph may have compatibility issues
# - This is NOT recommended for production
#
# SETUP:
# py -3.14 -m venv .venv
# .venv\Scripts\Activate.ps1
# pip install --upgrade pip setuptools wheel
# pip install -r requirements-314.txt

# ============================================
# WEB FRAMEWORK & SERVER
# ============================================
# Pinned to versions with 3.14 support
fastapi>=0.115.6
uvicorn[standard]>=0.32.1
sse-starlette>=2.1.3
pydantic>=2.10.3
python-dotenv>=1.0.1

# ============================================
# LLM & REASONING (LangChain/LangGraph Stack)
# ============================================
# Using flexible versions for 3.14 compatibility
langchain>=0.3.13
langchain-openai>=0.3.1
langchain-groq>=0.2.3
langgraph>=0.2.60
langsmith>=0.1.147

# ============================================
# LLM PROVIDERS
# ============================================
openai>=1.59.3
groq>=0.13.0
together>=1.3.11

# ============================================
# CHEMISTRY & BIOLOGY (3.14 EXPERIMENTAL)
# ============================================
# rdkit: May require source build for 3.14
# Try rdkit-pypi first; if fails, use conda:
#   conda install -c conda-forge rdkit=2024.3.6
rdkit-pypi>=2024.3.6
biopython>=1.84

# ============================================
# MOLECULAR DOCKING & STRUCTURE PREPARATION
# ============================================
vina>=1.2.5

# ============================================
# DATABASES & ASYNC
# ============================================
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.36

# ============================================
# HTTP REQUESTS
# ============================================
requests>=2.32.3
httpx>=0.28.1

# ============================================
# NUMERICAL & IMAGE PROCESSING
# ============================================
# numpy: Now supports 2.x for better 3.14 optimization
# Use numpy 2.x for Python 3.14+
numpy>=1.22.4
pillow>=10.3.0,<11.0.0
reportlab>=4.2.5

# ============================================
# SYSTEM DEPENDENCIES (install separately)
# ============================================
# openbabel - for SMILES/PDB/PDBQT conversion
#   Windows: choco install openbabel OR download MSI
#   Ubuntu/Debian: apt install openbabel
#   macOS: brew install open-babel
#
# fpocket - for pocket detection (optional)
#   Ubuntu/Debian: apt install fpocket
#   Note: Very limited 3.14 support